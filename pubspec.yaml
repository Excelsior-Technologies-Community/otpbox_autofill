name: otpbox_autofill
description: OTP input widget with SMS autofill support
version: 1.0.0

publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sms_autofill: ^2.4.1
